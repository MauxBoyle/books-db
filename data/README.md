# Data directories

- `unprocessed/` — place source files here before importing them.
- `processed/` — store files after they have been processed by the program.

The directories are intentionally tracked while their contents are not.
